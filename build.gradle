/*
 * This build file was generated by the Gradle 'init' task.
 *
 * For more details take a look at the Java Quickstart chapter in the Gradle
 * user guide available at https://docs.gradle.org/3.5/userguide/tutorial_java_projects.html
 */
apply plugin: 'java'

// name = "Albianj2"
// description = "Albianj2-分布式统框架"

task wrapper(type: Wrapper) {
    gradleVersion = '3.5'
}
repositories {
    jcenter()
    mavenCentral()
}
allprojects {
    group = "com.albianj2"
    version = "2.0.0"
}
subprojects {
    apply plugin: 'java'

    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8

    repositories {
        jcenter()
        mavenCentral()
    }
    artifacts {
        archives jar
    }

    configurations {
        deployerJars
    }
}

subprojects.each { subproject ->
    evaluationDependsOn(subproject.path)
}

task allJar (type: Jar, dependsOn: subprojects.assemble) {
    baseName = 'Albianj2-all-jars'
    subprojects.each { subproject ->
        from subproject.configurations.archives.allArtifacts.files.collect{ zipTree(it) }
    }
}

/*
dependencies {
    compile group: 'org.apache.commons', name: 'commons-lang3', version: '3.5'
    compile group: 'com.google.inject', name: 'guice', version: '4.1.0'
    compile group: 'com.google.guava', name: 'guava', version: '22.0'
    compile group: 'org.gradle', name: 'gradle-core', version: '3.5'
    compile group: 'org.gradle', name: 'gradle-logging', version: '3.5'
    compile group: 'org.gradle', name: 'gradle-tooling-api', version: '3.5'
    compile group: 'org.gradle', name: 'gradle-base-services', version: '3.5'
    compile group: 'org.codehaus.groovy', name: 'groovy-all', version: '2.4.11'
}
*/